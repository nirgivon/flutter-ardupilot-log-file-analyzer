import 'package:flutter/material.dart';

class ActionBar extends StatelessWidget {
  ActionBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('main action bar'),
    );
  }
}
